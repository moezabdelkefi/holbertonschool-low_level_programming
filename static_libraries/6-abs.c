#include "main.h"
/**
 * _abs - function that computes the apsolute value .
 *@n: letter input
 * Return: the absolute value of abs.
 */
int _abs(int n)
{

	if (n < 0)
		return (-n);
	else
		return (n);
}
