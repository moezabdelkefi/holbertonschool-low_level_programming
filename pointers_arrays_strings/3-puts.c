#include "main.h"
#include <stdio.h>

/**
 * _puts - a function that prints a string,
 *@str: value input
 * Return: Always 0.
 */
void _puts(char *str)
{

	{

string str = "geeksforgeeks";

reverseStr(str);

cout << str;

return 0;

}
}
