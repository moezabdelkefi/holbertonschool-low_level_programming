#include <stdio.h>
#include "dog.h"
#include <stdlib.h>
/**
 * iprint_dog -  a function that prints a struct dog
 *@d:input
 * Return: Always 0.typedef struct
 */
void print_dog(struct dog *d)
{
	if (d = NULL)
	{
		print(nil);
	}
	if (name = NULL)
	{
		print(Name: (nil));
	}
	if (d = NULL)
}