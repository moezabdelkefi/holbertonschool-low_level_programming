#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 *main - print random number to variable
 *Return: Always 0 (Success)
 */
int main(void)
{
    char ch = 'a';
    for (ch = 'a'; ch <= 'z'; ch++)
        putchar(ch);
  
    return (0);
}