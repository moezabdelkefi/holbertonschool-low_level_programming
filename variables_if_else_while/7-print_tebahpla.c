#include <stdio.h>
#include <stdio.h>
#include <stdlib.h>
/**
 *main -a program that prints the alphabet in lowercase
 *Return: Always 0 (Success)
 */
int main(void)
{
char letter;

	for (letter= 'z'; letter >= 'a'; i--)
		putchar(letter);
	putchar('\n');
	return (0);
}
